export class Saves {
  fortitude: number;
  reflex: number;
  will: number;

  constructor(fort: number, ref: number, will: number) {
    this.fortitude = fort;
    this.reflex = ref;
    this.will = will;
  }
}
